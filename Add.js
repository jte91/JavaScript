function addup(num)
{
    let sum = (num * (num + 1)) / 2;
    return sum;

}
//console.log(addup(13));
function addOne(num)
{
    let sum = (num + 1);
    return sum;
}
console.log(addOne(0));